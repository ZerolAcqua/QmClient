# 粒子效果开关移除记录

## 范围

- 删除客户端设置页中的两个粒子效果开关及对应配置。
- 删除客户端本地脚印粒子和远程脚印粒子的渲染路径。
- 删除客户端实时状态中的相关字段，以及中心服和身份服务的存储、转发和序列化支持。
- 删除相关翻译、配置迁移映射、提取缓存、产品说明和专门测试。
- 保留与这两个开关无关的 Tee 拖尾和背景粒子效果。

## 兼容行为

旧客户端继续上报历史字段时，身份服务会忽略这些未知字段；中心服在保存客户端上报和转发前丢弃它们，并在向客户端发送旧上游快照前再次过滤。当前身份服务的存储和输出结构已删除这些字段，新版本客户端也不再发送它们。

## 验证与部署

2026-09-18 完成验证：

- 客户端：`python qmclient_scripts/gate/check_gate.py --mode quick`，11 项通过；按仓库要求未编译客户端。
- 中心服：本地及服务器 `npm test`，36 项通过；`node --check` 通过。
- 身份服务：本地及服务器 `cargo test`，17 项通过；服务器 `cargo build --release` 成功。保留既有的未使用字段警告。
- 本次目标配置、界面文本和客户端字段的残留扫描通过；中心服仅保留忽略历史字段所需的过滤代码和兼容测试。
- 翻译严格校验未通过：当前工作树另有提取清单不同步、缺失翻译、中文源键和人工审核积压；本次目标词条已清除。Python 测试环境未安装 pytest，未运行相应测试。

按用户确认，使用当前本地服务目录完整替换服务器代码，保留运行数据与环境密钥。服务器为 `42.194.185.210`，运行服务于 2026-09-18 23:42（UTC+8）重启：

- `voicesrv.service`：PID `3102719`，状态 `active/running`；公网 `:9987/healthz` 返回正常，已有玩家重新上报。
- `qmclient-center-server.service`：PID `3102747`，状态 `active/running`；本机 `:8080/healthz` 返回正常。
- `node deploy/probe_realtime.js wss://qmclient.icu/ws` 通过：主连接初始主题、停止确认、协作建房/加入/推送/离开正常。
- 公网兼容探针通过：模拟旧客户端同时上报两个历史开关，服务接受上报，返回快照不包含这两个字段，身份与语音能力正常保留。
- 重启后的服务日志未见启动错误。客户端安装包未重新生成或替换，界面删除将在使用修改后的源码构建客户端后生效。
- 只读代码审查未发现本次粒子移除引入的问题。

部署前备份位于服务器 `/home/ubuntu/qmclient-particle-backup-20260918-232653`，包含原关键服务源码及身份服务可执行文件。
